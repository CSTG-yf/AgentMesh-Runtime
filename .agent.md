# .agent.md

你是 Codex，在本仓库中负责完整实现 AgentMesh-Runtime。

请严格遵守本文档中的项目目标、架构边界、技术选型、代码规范和验收标准。

---

## 1. 项目定位

本项目名为 AgentMesh-Runtime，是一个面向多智能体协作的轻量级运行时系统。

本项目不是普通多 Agent 应用，不是 LangChain 套壳，不是聊天机器人，不是单纯调用大模型 API 的 Demo。

本项目要实现的是：

```text
面向 Agent 间协作的协议化通信、非文本状态传递、共享记忆复用和可复现实验评测运行时。
```

核心思想是：

```text
从 Language-Passing 转向 State-Passing
```

也就是说，Agent 之间不要反复传递完整自然语言上下文，而是通过结构化协议传递：

- action；
- params；
- result；
- capability；
- state_refs；
- memory_refs；
- trace_id；
- error；
- created_at。

---

## 2. 最高优先级目标

你必须优先实现以下能力：

1. Agent Message Protocol，简称 AMP；
2. HELLO 和 CAPABILITY_ADVERTISE；
3. PlannerAgent、RetrieverAgent、ExecutorAgent、SummarizerAgent 四个 Agent；
4. Text Mode 纯文本协作模式；
5. Protocol Mode 结构化协议协作模式；
6. StateStore；
7. StateRef；
8. EmbeddingRef；
9. HashEmbedding；
10. State Lineage；
11. Shared Memory Store；
12. SQLite FTS 关键词检索；
13. 标签检索；
14. 语义检索；
15. MemoryScorer；
16. MemoryWritePolicy；
17. SandboxRunner；
18. Benchmark Runner；
19. experiment_report.md 自动生成；
20. openEuler 24.03-LTS-SP3 可运行部署脚本。

---

## 3. 技术约束

### 3.1 必须使用

- Python 3.11+
- uv
- SQLite
- Typer
- Pydantic v2
- Rich
- orjson
- PyYAML
- msgpack
- pytest
- ruff
- mypy

### 3.2 第一版不要使用

除非用户明确要求，否则不要引入：

- Redis
- Kafka
- RabbitMQ
- Milvus
- Faiss
- LangChain
- CrewAI
- AutoGen
- LlamaIndex
- torch
- sentence-transformers
- numpy
- pandas
- docker-only 方案
- 外部大模型 API 强依赖

### 3.3 关键原因

本项目最终必须能在 openEuler 24.03-LTS-SP3 上稳定运行。第一版应尽量避免重型二进制依赖，保证：

```bash
uv sync
uv run pytest
uv run agentmesh benchmark --suite examples/benchmarks/continuous_tasks.yaml
uv run agentmesh report --run runs/latest
```

能稳定通过。

---

## 4. 仓库结构

请按以下结构实现：

```text
agentmesh-runtime/
├── pyproject.toml
├── uv.lock
├── README.md
├── .agent.md
├── Makefile
├── scripts/
│   ├── setup_openeuler.sh
│   ├── run_demo.sh
│   └── run_tests.sh
├── src/
│   └── agentmesh/
│       ├── __init__.py
│       ├── cli.py
│       ├── protocol/
│       │   ├── __init__.py
│       │   ├── enums.py
│       │   ├── schema.py
│       │   ├── codec.py
│       │   ├── router.py
│       │   └── capability.py
│       ├── runtime/
│       │   ├── __init__.py
│       │   ├── agent.py
│       │   ├── registry.py
│       │   ├── scheduler.py
│       │   ├── event_bus.py
│       │   └── orchestrator.py
│       ├── agents/
│       │   ├── __init__.py
│       │   ├── planner.py
│       │   ├── retriever.py
│       │   ├── executor.py
│       │   └── summarizer.py
│       ├── state/
│       │   ├── __init__.py
│       │   ├── schema.py
│       │   ├── store.py
│       │   ├── refs.py
│       │   ├── embedding.py
│       │   └── lineage.py
│       ├── memory/
│       │   ├── __init__.py
│       │   ├── schema.py
│       │   ├── sqlite_store.py
│       │   ├── search.py
│       │   ├── scorer.py
│       │   └── policy.py
│       ├── modes/
│       │   ├── __init__.py
│       │   ├── text_mode.py
│       │   └── protocol_mode.py
│       ├── sandbox/
│       │   ├── __init__.py
│       │   ├── runner.py
│       │   └── limits.py
│       ├── eval/
│       │   ├── __init__.py
│       │   ├── metrics.py
│       │   ├── benchmark.py
│       │   ├── report.py
│       │   └── quality.py
│       └── storage/
│           ├── __init__.py
│           ├── paths.py
│           └── jsonl.py
├── examples/
│   ├── tasks/
│   ├── corpora/
│   └── benchmarks/
├── tests/
└── docs/
```

不要随意改目录结构。如果确实需要新增目录，必须保持清晰、轻量、可测试。

---

## 5. pyproject.toml 要求

使用如下依赖：

```toml
[project]
name = "agentmesh-runtime"
version = "0.1.0"
description = "A lightweight runtime for structured multi-agent communication, state passing and shared memory."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7.0",
    "typer>=0.12.0",
    "rich>=13.7.0",
    "orjson>=3.10.0",
    "pyyaml>=6.0.1",
    "msgpack>=1.0.8",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
]

dashboard = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
agentmesh = "agentmesh.cli:app"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --disable-warnings"
```

---

## 6. 编码规范

### 6.1 基本要求

- 所有核心数据结构使用 Pydantic BaseModel。
- 所有公共函数必须有类型注解。
- 文件读写必须显式指定 UTF-8。
- 所有 JSONL 写入统一走 `storage/jsonl.py`。
- 所有路径统一走 `storage/paths.py`。
- 所有 CLI 命令必须能给出清晰错误信息。
- 所有测试必须可重复运行。
- 不要依赖当前工作目录，路径必须可配置。
- 不要在测试中访问网络。
- 不要把外部 API 作为测试前提。
- 不要把大量长文本硬编码在 Python 代码里，应放入 examples/tasks 或 examples/corpora。

### 6.2 错误处理

必须定义清晰错误：

- ProtocolError
- StateNotFoundError
- MemoryNotFoundError
- CapabilityNotFoundError
- SandboxTimeoutError
- BenchmarkConfigError

可以放在：

```text
src/agentmesh/errors.py
```

---

## 7. AMP 协议实现要求

### 7.1 MsgType

实现：

```python
from enum import StrEnum

class MsgType(StrEnum):
    HELLO = "HELLO"
    CAPABILITY_ADVERTISE = "CAPABILITY_ADVERTISE"
    CAPABILITY_QUERY = "CAPABILITY_QUERY"
    PROTOCOL_MAP = "PROTOCOL_MAP"
    INVOKE = "INVOKE"
    RESULT = "RESULT"
    ERROR = "ERROR"
    MEMORY_PUT = "MEMORY_PUT"
    MEMORY_QUERY = "MEMORY_QUERY"
    STATE_REF = "STATE_REF"
    TRACE_EVENT = "TRACE_EVENT"
```

### 7.2 AMPMessage

实现：

```python
class AMPMessage(BaseModel):
    message_id: str
    trace_id: str
    source_agent: str
    target_agent: str
    msg_type: MsgType
    action: str | None
    params: dict[str, Any]
    result: dict[str, Any]
    capability: list[str]
    state_refs: list[str]
    created_at: datetime
```

要求：

- message_id 默认 `msg-<12位hex>`；
- created_at 使用 UTC；
- `msg_type=INVOKE` 时 action 不能为空；
- `msg_type=RESULT` 时 result 不能为空；
- `msg_type=ERROR` 时 result 里必须有 error_code 和 message；
- state_refs 必须是 `state://` 开头；
- codec 支持 orjson 序列化和反序列化。

---

## 8. Agent 实现要求

### 8.1 BaseAgent

每个 Agent 必须包含：

```python
name: str
capabilities: list[str]
handle(message, context) -> AMPMessage
hello() -> AMPMessage
advertise_capabilities() -> AMPMessage
```

### 8.2 Agent 列表

必须实现：

- PlannerAgent
- RetrieverAgent
- ExecutorAgent
- SummarizerAgent

### 8.3 能力要求

PlannerAgent：

```text
plan.create
protocol.map
memory.query
```

RetrieverAgent：

```text
memory.keyword_search
memory.tag_search
memory.semantic_search
evidence.collect
```

ExecutorAgent：

```text
tool.run_python
tool.validate_result
state.consume
```

SummarizerAgent：

```text
summary.create
memory.put
report.fragment
```

### 8.4 握手流程

运行 Protocol Mode 时必须先输出：

```text
HELLO
CAPABILITY_ADVERTISE
```

消息必须写入：

```text
runs/latest/protocol/messages.jsonl
```

---

## 9. StateStore 实现要求

### 9.1 StateType

必须支持：

- text
- embedding
- summary
- evidence
- code_result
- blob
- hidden

### 9.2 StateRecord

必须包含：

```text
state_id
trace_id
state_type
producer
consumers
parent_state_refs
payload_ref
size_bytes
metadata
created_at
```

### 9.3 StateRef

格式必须为：

```text
state://<state_type>/<state_id>
```

例如：

```text
state://text/state-a1b2c3d4
state://embedding/state-e5f6g7h8
state://code_result/state-i9j0k1l2
```

### 9.4 StateStore 功能

必须实现：

```text
put_text
put_embedding
put_summary
put_evidence
put_code_result
put_blob
get
exists
add_consumer
list_by_trace
```

### 9.5 State Lineage

必须记录：

- 当前状态由哪个 Agent 生成；
- 当前状态依赖哪些 parent_state_refs；
- 当前状态被哪些 Agent 消费；
- 当前状态属于哪个 trace_id；
- 当前状态是否被写入长期记忆。

---

## 10. HashEmbedding 实现要求

不要使用外部模型。第一版实现 HashEmbeddingEncoder。

要求：

```text
输入：str
输出：list[float]
默认维度：384
确定性：同一输入多次输出完全一致
归一化：L2 normalize
相似度：cosine_similarity
```

建议算法：

```text
1. lower
2. regex 分词
3. sha256 token
4. hash 到 384 个 bucket
5. 根据 hash 奇偶添加 +1 或 -1
6. L2 normalize
```

测试要求：

- 同一文本 embedding 完全一致；
- 相似文本 cosine 高于无关文本；
- 空文本不会崩溃；
- embedding 能写入 StateStore 并读回。

---

## 11. Shared Memory Store 实现要求

### 11.1 MemoryUnit

必须包含：

```text
memory_id
source_agent
created_at
last_used_at
task_topic
summary
tags
evidence_refs
state_refs
embedding_ref
reuse_count
confidence
validity_score
reuse_policy
provenance_trace_id
```

### 11.2 SQLite 表

必须创建：

```sql
CREATE TABLE IF NOT EXISTS memory_units (
    memory_id TEXT PRIMARY KEY,
    source_agent TEXT NOT NULL,
    created_at TEXT NOT NULL,
    last_used_at TEXT,
    task_topic TEXT NOT NULL,
    summary TEXT NOT NULL,
    tags_json TEXT NOT NULL,
    evidence_refs_json TEXT NOT NULL,
    state_refs_json TEXT NOT NULL,
    embedding_ref TEXT,
    reuse_count INTEGER DEFAULT 0,
    confidence REAL DEFAULT 0,
    validity_score REAL DEFAULT 0,
    reuse_policy TEXT DEFAULT 'verify',
    provenance_trace_id TEXT NOT NULL
);
```

必须创建 FTS 表：

```sql
CREATE VIRTUAL TABLE IF NOT EXISTS memory_fts
USING fts5(memory_id, task_topic, summary, tags);
```

### 11.3 检索 API

必须实现：

```text
put_memory
get_memory
search_keyword
search_tag
search_semantic
update_reuse_count
list_recent
```

### 11.4 MemoryScorer

实现：

```text
score =
  0.45 * semantic_similarity
+ 0.20 * tag_match_score
+ 0.15 * confidence
+ 0.15 * evidence_coverage
- 0.05 * time_decay
```

返回结果中必须包含：

```text
memory_id
score
semantic_similarity
tag_match_score
confidence
evidence_coverage
time_decay
```

这样方便答辩展示系统不是盲目复用记忆。

---

## 12. SandboxRunner 实现要求

文件：

```text
src/agentmesh/sandbox/runner.py
```

要求：

```text
1. 使用 tempfile.TemporaryDirectory
2. 把代码写入 main.py
3. 使用 subprocess.run 执行 python3 main.py
4. 限制 timeout，默认 5 秒
5. 限制 stdout/stderr 长度，默认 8000 字符
6. 捕获 exit_code、stdout、stderr、latency_ms、timeout
7. 结果写入 CodeResultState
```

不要直接把大段 stdout 传给下一个 Agent。必须通过 StateRef 传递。

---

## 13. Text Mode 实现要求

文件：

```text
src/agentmesh/modes/text_mode.py
```

要求：

```text
1. Agent 间通过完整自然语言上下文传递信息。
2. 每一步把已有上下文全部拼接到下一步。
3. 记录 text_chars。
4. 估算 estimated_tokens = max(1, text_chars // 4)。
5. 写入 runs/latest/text/messages.jsonl。
6. 写入 runs/latest/text/trace.jsonl。
7. 不使用 StateRef。
```

必须保留 Text Mode 的上下文膨胀特征，作为 baseline。

---

## 14. Protocol Mode 实现要求

文件：

```text
src/agentmesh/modes/protocol_mode.py
```

流程：

```text
1. 读取用户任务。
2. 用户任务写入 TextState。
3. 生成 query EmbeddingState。
4. PlannerAgent 生成 task_plan SummaryState。
5. PlannerAgent 发 INVOKE 给 RetrieverAgent。
6. RetrieverAgent 根据 query_ref / tags 检索 MemoryStore。
7. RetrieverAgent 写 EvidenceState。
8. ExecutorAgent 根据 state_refs 执行工具或代码。
9. ExecutorAgent 写 CodeResultState。
10. SummarizerAgent 读取 evidence_refs 和 execution_result_ref。
11. SummarizerAgent 生成 final_answer。
12. SummarizerAgent 写 MemoryUnit。
13. 全部 AMPMessage 写入 messages.jsonl。
14. 全部 StateRecord 写入 states.jsonl。
15. 全部 MemoryUnit 写入 memory.jsonl。
```

要求：

- 不允许在 Agent 间直接传递完整长文本。
- 可以在 StateStore 内保存长文本，但 AMPMessage 中只能放引用。
- Protocol Mode 中必须产生至少一个 TextState、EmbeddingState、EvidenceState、CodeResultState、SummaryState。
- Protocol Mode 中必须产生至少一个 MemoryUnit。

---

## 15. Benchmark Runner 实现要求

文件：

```text
src/agentmesh/eval/benchmark.py
```

要求：

```text
1. 读取 examples/benchmarks/continuous_tasks.yaml。
2. 对每个任务分别运行 Text Mode 和 Protocol Mode。
3. 使用相同任务输入。
4. 使用相同 Agent 配置。
5. 使用相同初始共享记忆。
6. 每个任务支持 repeat。
7. 输出 benchmark_summary.csv。
8. 输出 benchmark_detail.jsonl。
```

必须统计：

```text
task_id
mode
repeat_index
message_count
text_chars
estimated_tokens
protocol_bytes
state_transfer_count
state_transfer_bytes
total_latency_ms
memory_query_count
memory_hit_count
memory_hit_rate
task_success
answer_quality_score
evidence_coverage
memory_reuse_accuracy
```

必须计算：

```text
TokenSavingRate
LatencyReductionRate
MemoryHitRate
QualityPreservationRate
```

---

## 16. Report Generator 实现要求

文件：

```text
src/agentmesh/eval/report.py
```

生成：

```text
runs/latest/experiment_report.md
```

报告必须包含：

```text
1. 实验环境
2. 任务列表
3. Text Mode 与 Protocol Mode 指标对比表
4. TokenSavingRate
5. LatencyReductionRate
6. MemoryHitRate
7. QualityPreservationRate
8. 协议通信日志样例
9. StateRef 状态流样例
10. MemoryUnit 样例
11. 结论
```

---

## 17. CLI 命令要求

文件：

```text
src/agentmesh/cli.py
```

必须实现：

```bash
agentmesh init

agentmesh run --mode protocol --task examples/tasks/A1_requirements.txt

agentmesh run --mode text --task examples/tasks/A1_requirements.txt

agentmesh benchmark --suite examples/benchmarks/continuous_tasks.yaml

agentmesh memory search --keyword protocol

agentmesh memory search --tag shared-memory

agentmesh memory search --semantic "agent state passing"

agentmesh trace show --run runs/latest

agentmesh report --run runs/latest
```

CLI 输出要清晰，建议使用 Rich。

---

## 18. 测试要求

必须实现以下测试：

```text
tests/test_protocol_schema.py
tests/test_capability_handshake.py
tests/test_state_store.py
tests/test_embedding_ref.py
tests/test_memory_store.py
tests/test_sandbox_runner.py
tests/test_text_mode.py
tests/test_protocol_mode.py
tests/test_benchmark.py
```

每个测试必须：

- 不访问网络；
- 不依赖真实外部 API；
- 使用 tmp_path；
- 可重复运行；
- 不污染用户目录；
- 尽量覆盖异常路径。

---

## 19. openEuler 部署脚本

实现：

```text
scripts/setup_openeuler.sh
```

内容要求：

```bash
#!/usr/bin/env bash
set -euo pipefail

sudo dnf update -y

sudo dnf install -y \
  python3 \
  python3-devel \
  gcc \
  gcc-c++ \
  make \
  sqlite \
  sqlite-devel \
  git \
  curl

if ! command -v uv >/dev/null 2>&1; then
  curl -LsSf https://astral.sh/uv/install.sh | sh
  export PATH="$HOME/.local/bin:$PATH"
fi

python3 --version
sqlite3 --version
uv --version

uv sync --all-extras
uv run pytest
```

---

## 20. Makefile 要求

实现：

```makefile
.PHONY: sync test lint typecheck demo benchmark report all

sync:
	uv sync --all-extras

test:
	uv run pytest

lint:
	uv run ruff check .

typecheck:
	uv run mypy src

demo:
	uv run agentmesh run --mode protocol --task examples/tasks/A1_requirements.txt

benchmark:
	uv run agentmesh benchmark --suite examples/benchmarks/continuous_tasks.yaml

report:
	uv run agentmesh report --run runs/latest

all: sync lint typecheck test benchmark report
```

最终必须保证：

```bash
make all
```

可以通过。

---

## 21. 开发顺序

严格按这个顺序开发，不要跳步：

```text
阶段 0：仓库初始化
阶段 1：AMP 协议
阶段 2：Agent 注册与能力发现
阶段 3：StateStore / StateRef
阶段 4：HashEmbedding / EmbeddingRef
阶段 5：Shared Memory Store
阶段 6：SandboxRunner
阶段 7：Protocol Mode
阶段 8：Text Mode
阶段 9：Benchmark Runner
阶段 10：Report Generator
阶段 11：openEuler 部署文档与脚本
阶段 12：README 与答辩样例输出
```

每完成一个阶段都运行：

```bash
uv run pytest
uv run ruff check .
```

---

## 22. 每阶段验收命令

### 阶段 0

```bash
uv sync
uv run agentmesh --help
uv run pytest
```

### 阶段 1

```bash
uv run pytest tests/test_protocol_schema.py
```

### 阶段 2

```bash
uv run pytest tests/test_capability_handshake.py
uv run agentmesh run --mode protocol --task examples/tasks/A1_requirements.txt
```

### 阶段 3

```bash
uv run pytest tests/test_state_store.py
```

### 阶段 4

```bash
uv run pytest tests/test_embedding_ref.py
```

### 阶段 5

```bash
uv run pytest tests/test_memory_store.py
```

### 阶段 6

```bash
uv run pytest tests/test_sandbox_runner.py
```

### 阶段 7

```bash
uv run pytest tests/test_protocol_mode.py
uv run agentmesh run --mode protocol --task examples/tasks/A1_requirements.txt
```

### 阶段 8

```bash
uv run pytest tests/test_text_mode.py
uv run agentmesh run --mode text --task examples/tasks/A1_requirements.txt
```

### 阶段 9

```bash
uv run pytest tests/test_benchmark.py
uv run agentmesh benchmark --suite examples/benchmarks/continuous_tasks.yaml
```

### 阶段 10

```bash
uv run agentmesh report --run runs/latest
```

### 最终验收

```bash
uv sync --all-extras
uv run ruff check .
uv run mypy src
uv run pytest
uv run agentmesh benchmark --suite examples/benchmarks/continuous_tasks.yaml
uv run agentmesh report --run runs/latest
make all
```

---

## 23. 输出文件要求

运行 Benchmark 后必须生成：

```text
runs/latest/
├── benchmark_summary.csv
├── benchmark_detail.jsonl
├── experiment_report.md
├── text/
│   ├── messages.jsonl
│   └── trace.jsonl
└── protocol/
    ├── messages.jsonl
    ├── states.jsonl
    ├── memory.jsonl
    └── trace.jsonl
```

---

## 24. 答辩展示要求

最终项目必须能展示：

### 24.1 协议通信日志

必须看到：

```text
HELLO
CAPABILITY_ADVERTISE
INVOKE
RESULT
MEMORY_PUT
STATE_REF
```

### 24.2 StateRef 状态流

必须看到：

```text
state://text/...
state://embedding/...
state://evidence/...
state://code_result/...
```

### 24.3 共享记忆检索

必须能运行：

```bash
agentmesh memory search --keyword protocol
agentmesh memory search --tag shared-memory
agentmesh memory search --semantic "state passing"
```

### 24.4 Benchmark 对比

必须能在 `experiment_report.md` 中看到：

```text
TokenSavingRate
LatencyReductionRate
MemoryHitRate
QualityPreservationRate
```

---

## 25. 严禁事项

不要做以下事情：

1. 不要把 Agent 协作全部写成自然语言字符串传递。
2. 不要只做一个聊天机器人。
3. 不要把项目变成 LangChain 调用 Demo。
4. 不要要求用户必须配置大模型 API Key 才能运行测试。
5. 不要在测试中访问网络。
6. 不要把 StateRef 当成普通字符串展示而不实现真实读取。
7. 不要只写文档不写测试。
8. 不要只实现 Protocol Mode，不实现 Text Mode。
9. 不要只实现 Text Mode，不实现 Protocol Mode。
10. 不要只输出最终答案，不记录 trace、message log、state log、memory log。
11. 不要让 Benchmark 依赖随机结果。
12. 不要忽略 openEuler 兼容性。
13. 不要引入重型依赖破坏 uv sync。
14. 不要在代码里硬编码绝对路径。
15. 不要把所有逻辑堆到 cli.py。

---

## 26. 最小可行产品定义

MVP 完成标准：

```text
1. uv sync 成功；
2. uv run pytest 成功；
3. uv run agentmesh run --mode protocol --task examples/tasks/A1_requirements.txt 成功；
4. uv run agentmesh run --mode text --task examples/tasks/A1_requirements.txt 成功；
5. uv run agentmesh benchmark --suite examples/benchmarks/continuous_tasks.yaml 成功；
6. uv run agentmesh report --run runs/latest 成功；
7. runs/latest 中存在 benchmark_summary.csv、benchmark_detail.jsonl、experiment_report.md；
8. protocol/messages.jsonl 中存在 HELLO、CAPABILITY_ADVERTISE、INVOKE、RESULT；
9. protocol/states.jsonl 中存在 TextState、EmbeddingState、EvidenceState、CodeResultState；
10. protocol/memory.jsonl 中存在 MemoryUnit；
11. README 说明 openEuler 24.03-LTS-SP3 部署方式；
12. make all 成功。
```

---

## 27. 给 Codex 的最终实现目标

请实现一个名为 AgentMesh-Runtime 的 Python 项目，使用 uv 管理依赖，要求能在 openEuler 24.03-LTS-SP3 上运行。项目需要实现多 Agent 协作运行时，而不是简单 Agent 应用。核心包括：Agent Message Protocol 结构化通信协议、HELLO 和 CAPABILITY_ADVERTISE 能力发现、Text Mode 和 Protocol Mode 两种协作模式、StateStore 和 StateRef 非文本状态传递、HashEmbedding 语义向量、Shared Memory Store 共享记忆、SQLite FTS 关键词检索、标签检索、语义检索、CodeAct SandboxRunner、Benchmark Runner 和自动实验报告生成。所有核心模块必须有 pytest 测试，最终能通过 uv sync、uv run pytest、uv run agentmesh benchmark 和 uv run agentmesh report。
